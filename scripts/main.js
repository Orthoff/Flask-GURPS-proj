// Store a reference to the <h1> in a variable
const myHeading = document.querySelector("h1");
// Update the text content of the <h1>
myHeading.textContent = "Hello world!";
console.log(document.title);

const newCharButton = document.getElementById("new-char");
const viewCharsButton = document.getElementById("my-chars");
const shareCharsButton = document.getElementById("share-chars");
 
newCharButton.addEventListener("click", )

// attempt at a factory funtion smh
function charCreator(){
    return {
        
    }
}